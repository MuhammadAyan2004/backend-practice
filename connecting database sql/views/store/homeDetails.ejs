<%- include('../partials/head.ejs') %>
</head>

<body>

  <%- include('../partials/nav.ejs') %>
  <main class="min-h-screen flex items-center justify-center flex-col p-6 gap-5">
    <h1 class="text-3xl text-center"> you are in home detail page</h1>
    <div class="max-w-3xl w-full bg-white rounded-2xl shadow-xl overflow-hidden">
      <!-- Home Image -->
      <div class="h-80 w-full rounded-4xl">
        <img src="<%= home.photoUrl %>" 
             alt="<%= home.houseName %>" 
             class="w-full h-full object-cover rounded-[10px]">
      </div>

      <!-- Home Info -->
      <div class="p-6 space-y-4">
        <h1 class="text-3xl font-bold text-gray-900"><%= home.houseName %></h1>
        <p class="text-gray-600 flex items-center">📍 <span class="ml-2"><%= home.location %></span></p>
        <p class="text-yellow-500 font-semibold">⭐ <%= home.rating %></p>
        <p class="text-blue-600 text-xl font-bold">💰 $<%= home.price %> / night</p>
      </div>

      <!-- Action Buttons -->
      <div class="px-6 pb-6 flex gap-4">
        <a href="/homeList" 
           class="w-[20vh] text-center bg-gray-400 text-gray-100 font-semibold py-3 rounded-xl hover:bg-gray-600 transition">
          Back
        </a>
        <%- include('../partials/fav.ejs') %>
      </div>
    </div>
  </main>

</body>

</html>